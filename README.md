# Trello Synchronizer

#### Node JS dependencies
* npm install oauth
* npm install mssql
* npm install express
* npm install properties-reader
* npm install serve-static
* npm install log4js
* npm install iconv-lite
